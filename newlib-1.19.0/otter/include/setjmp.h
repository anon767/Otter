#ifndef _SETJMP_H_
#define _SETJMP_H_

/* 
 * Otter's setjmp.h
 *
 * This header, together with machine/setjmp.h, 
 * must shield the ones inside newlib, 
 * as these two pairs are not compatible.
 */
#include <machine/setjmp.h>

#endif
