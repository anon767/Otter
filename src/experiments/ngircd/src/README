To merge:
1. Extract the tarball into this directory.
2. Run "make conf VERSION=<directory name without ending slash>".
3. Run "make VERSION=<directory name without ending slash>".

Unless there're changes to how it's built, you don't need to run make conf again.
